# How to run

To run the sort function you can run the command below in the terminal:
```
python sort.py [width, height, length, mass]
```

# How to test

To test the sort function you can run the command below in the terminal:
```
python -m unittest test_sort.py
```